string reverseWord(string str){
    
  //Your code here
  int n = str.length();
  
  for (int i; i<n/2; i++){
     int temp=str[i];
     str[i]=str[n-i-1];
     str[n-i-1]=temp;
  }
  
  return str;
}
